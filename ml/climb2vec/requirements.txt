# Offline Climb2Vec training/eval deps. Kept minimal and CPU-only so the pipeline
# runs anywhere; it never ships to prod (serving is pure TS, per docs/climb2vec.md).
numpy>=1.26
scipy>=1.11
scikit-learn>=1.4
# torch is required for Runs 5–7 (and the legacy Deep Sets encoder). Install the
# CPU wheel separately so this file stays portable across CPU/CUDA hosts:
# pip install torch --index-url https://download.pytorch.org/whl/cpu
