// Notifications pushed inside the Profile tab stack, so a route opened from the
// You tab keeps the native tab bar and returns there on Back. Shares the same
// screen component as the Home tab's notifications route.
export { default } from '../../../src/components/notifications/NotificationsScreen';
