// Notifications pushed inside the Home tab stack, so the bell in the Home chrome
// keeps the native tab bar on screen and Back returns to the feed rather than
// dumping the user in the You tab. Shares the same screen component as the
// Profile tab's notifications route.
export { default } from '../../../src/components/notifications/NotificationsScreen';
