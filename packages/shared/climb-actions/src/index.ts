// Pure TS primitives shared by the web and mobile climb-action providers
// (favorites today; playlists could follow when their cache logic is worth
// deduplicating). No React, no DOM, no React Native.

export { FavoritesStore, favoritesStore } from './favorites-store';
