// Re-export for ergonomic imports — keeps consumers from having to know
// whether the hook lives in a context file vs a dedicated hook file.
export { useBoardHistory } from './board-history-context';
