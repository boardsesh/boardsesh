# Backend API URL (GraphQL + Auth)
VITE_API_URL=http://localhost:4000

# WebSocket URL for GraphQL subscriptions (optional - derived from VITE_API_URL if not set)
VITE_WS_URL=ws://localhost:4000/graphql
