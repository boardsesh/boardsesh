[package]
name = "board-renderer-core"
version = "0.1.0"
description = "Aura board renderer core: hold geometry, veil, glow and marker passes (tiny-skia)"
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
tiny-skia = "0.11"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
